#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

#include <stdio.h>

#include <stdio.h>

int main() {
    int N, i, searchelement, found = 0;
    scanf("%d", &N);
    int arr[N];
    for(i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }
    scanf("%d", &searchelement);
    for(i = 0; i < N; i++) {
        if(arr[i] == searchelement) {
            found = 1;
            break;
        }
    }
    if(found) {
        printf("Found\n");
    } else {
        printf("Not Found\n");
    }

    return 0;
}

